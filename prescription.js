document.addEventListener('DOMContentLoaded',()=>{const file=document.getElementById('prescriptionFile'),p=document.getElementById('prescriptionPreview'),btn=document.getElementById('analysePrescription'),o=document.getElementById('prescriptionOutput');if(!file)return;file.addEventListener('change',()=>{const f=file.files[0];if(f){const u=URL.createObjectURL(f);p.innerHTML=`<img src="${u}" alt="Uploaded prescription preview">`}});btn.addEventListener('click',()=>{if(!file.files[0]){o.innerHTML='<div class="notice">Please upload an image first.</div>';return}o.innerHTML='<div class="medicine-card"><span class="confidence">Prototype OCR • Needs Verification</span><h3>Extraction workflow ready</h3><p>A production OCR/AI backend can extract only visible information: medicine name, strength, instructions, manufacturing date, expiry date and batch number.</p><p><strong>Never guess missing information.</strong> PRANAV does not independently prescribe or calculate a dose.</p></div>'})});
