document.addEventListener('DOMContentLoaded',()=>{const f=document.getElementById('medicineForm'),o=document.getElementById('medicineResult');if(!f)return;f.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(f);o.innerHTML=`<div class="medicine-card"><span class="confidence">Information Review</span><h3>${d.get('name')}</h3><p><strong>Strength:</strong> ${d.get('strength')||'Not provided'}</p><p><strong>Manufacturing:</strong> ${d.get('mfg')||'Not provided'}</p><p><strong>Expiry:</strong> ${d.get('expiry')||'Not provided'}</p><p><strong>Patient age:</strong> ${d.get('age')||'Not provided'}</p><p><strong>Prescription dose:</strong> ${d.get('dose')||'Not provided'}</p><p><strong>Instructions:</strong> ${d.get('instructions')||'Not provided'}</p><div class="notice">PRANAV does not independently calculate or prescribe a dose. Follow the prescription or confirm with a qualified doctor/pharmacist. Check the physical packaging for expiry and storage information.</div></div>`})});
