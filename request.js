document.addEventListener('DOMContentLoaded',()=>{const f=document.getElementById('trackForm'),o=document.getElementById('trackResult');if(!f)return;f.addEventListener('submit',e=>{e.preventDefault();const id=new FormData(f).get('id').trim(),r=JSON.parse(localStorage.getItem('pranavEmergencyRequest')||'null');o.innerHTML=r&&r.id===id?`<div class="success-box"><h2>${r.id}</h2><p>${r.bloodGroup} • ${r.component} • ${r.city}</p><div class="timeline"><div class="active">1. Request Created</div><div>2. Requirement Analysed</div><div>3. Searching</div><div>4. Verification</div><div>5. Coordination</div></div></div>`:`<div class="notice">Request not found in this prototype browser. Verify the ID and facility status.</div>`})});
